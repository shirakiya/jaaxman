[Unit]
Description=uWSGI
After=syslog.target

[Service]
EnvironmentFile=/root/env
ExecStart=/root/local/python-{{ python_version }}/bin/uwsgi --ini /etc/nginx/uwsgi.ini
Restart=always
KillSignal=SIGQUIT
Type=notify
StandardError=syslog
NotifyAccess=all

[Install]
WantedBy=multi-user.target

/var/log/nginx/jaaxman/*.log {
    notifempty
    missingok
    daily
    dateext
    rotate 14
    su root root
    create 0644 root root
    sharedscripts
    postrotate
        [ ! -f /var/run/nginx.pid ] || kill -USR1 `cat /var/run/nginx.pid`
    endscript
}
